import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
err : {
display : "flex",
alignItems : "center",
justifyContent: "center",
fontSize : "40pt",
fontFamily : "Oswald"

}

}));